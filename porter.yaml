version: v1
resources:
  - name: "main2"
    source:
      name: "web"
    config:
      build:
        method: "pack"
      values:
        container:
          command: "npm run start"
          port: 3000
