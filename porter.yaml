# test
version: v1
resources:
  - name: "main"
    source:
      name: "web"
      repo: https://charts.dev.getporter.dev
    config:
      build:
        method: "pack"
      values:
        ingress:
          enabled: false
        container:
          command: "npm run start"
          port: 3000