version: v1
resources:
  - name: my-web-app
    depends_on:
      - worker
    source:
      name: web
    config:
      build:
        method: pack
      values:
        container:
          port: 8080
          env:
            normal:
              FOO: BAR
