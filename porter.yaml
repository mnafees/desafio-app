# just test
version: v1
resources:
  - name: my-web-app
    source:
      name: web
    config:
      build:
        method: pack
      values:
        container:
          port: 8080
          env:
            normal:
              FOO: BAR
